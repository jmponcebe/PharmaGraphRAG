# Minimal requirements for Streamlit Community Cloud deployment (pip fallback).
# Primary dependency management uses uv sync from uv.lock / pyproject.toml.
#
# In API mode (API_URL set), the Streamlit app only needs UI packages
# and requests — all data access goes through the remote FastAPI service.

streamlit>=1.41
streamlit-agraph>=0.0.45
pyvis>=0.3
plotly>=5.24
requests>=2.32
loguru>=0.7
