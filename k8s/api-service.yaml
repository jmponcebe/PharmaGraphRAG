apiVersion: v1
kind: Service
metadata:
  name: pharmagraphrag-api
  namespace: pharmagraphrag
  labels:
    app.kubernetes.io/name: pharmagraphrag
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: pharmagraphrag
    app.kubernetes.io/component: api
